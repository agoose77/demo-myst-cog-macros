# Using cog for cross-file macro sync

This demo uses [`cog`](https://cog.readthedocs.io/en/latest/index.html) to keep individual pages in-sync with a source definition. This is a manual code-generation process that works because it's so stupid!

```shell
pip install -r requirements.txt

./sync.sh **/*.md
```
